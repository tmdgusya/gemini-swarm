description = "Release a lock on a file path"
prompt = """
The user wants to release a file lock. Parse their request: {{args}}

Extract:
1. The file path to unlock
2. The agent name releasing the lock

Then call the `swarm_unlock` tool with:
- path: the file path to unlock
- agent: the agent name releasing the lock

Report whether the lock was successfully released.
"""
