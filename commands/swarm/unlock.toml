name = "swarm_unlock"
description = "Release a lock on a file path."

[inputSchema]
type = "object"
required = ["path", "agent"]

[inputSchema.properties.path]
type = "string"
description = "File path to unlock"

[inputSchema.properties.agent]
type = "string"
description = "Agent name releasing the lock"
