{
  "name": "gemini-swarm",
  "version": "0.1.0",
  "description": "Gemini CLI swarm mode extension — tmux + JSONL based",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.3"
  }
}
